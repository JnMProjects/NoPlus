import { Canvas, Meta, Primary, Controls, Source } from '@storybook/blocks';
import * as StatusDotStories from "./StatusDot.stories"

<Meta of={StatusDotStories} />

# StatusDot

## The Status Dot is used to Display the Status of a Proccess to a User instead of showing a Loader

<Primary />