import { Canvas, Meta, Primary, Controls, Source } from '@storybook/blocks';
import * as AspectRatioStories from './AspectRatio.stories';

<Meta of={AspectRatioStories} />

# AspectRatio
## AspectRatio is used to Controll the aspect ratio of a container
### For Example: Controling The Ratio of a Picture

<Primary />